{
  "name": "applyonce-webextension",
  "version": "1.3.1",
  "private": true,
  "description": "Shared Safari, Chrome, and Edge ApplyOnce WebExtension.",
  "scripts": {
    "build:chromium": "node scripts/build-extension.js",
    "package:chromium": "npm run build:chromium && cd dist/chromium && zip -qr ../ApplyOnce-chromium.zip .",
    "test": "node --test tests/extension.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
